# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Tarot Game API

# CORS Origins (comma-separated)
# Use "*" for development only, restrict for production
# Example: http://localhost:3000,http://localhost:8080,https://yourdomain.com
BACKEND_CORS_ORIGINS=*

# Debug Mode (1=enabled, 0=disabled)
# WARNING: Set to 0 in production
DEBUG=1

# Supabase Configuration
# Get these from your Supabase project settings > API
# Use service_role key for backend (full access, keep secret!)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-secret-key-here
